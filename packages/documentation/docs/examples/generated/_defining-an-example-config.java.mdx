```java
        contract.runInteraction(
                new InteractionDefinition<>(
                        List.of(
                                /* as above */
                        ,
                        /* as above */
                                .build()),
                IndividualSuccessTestConfigBuilder.builder().withLogLevel(LogLevel.DEBUG).build());
```
