```java
        contract.runInteraction(
                new InteractionDefinition<>(
                        List.of(
                                new InState("Server is up"),
                                new InState("A user with id \"foo\" exists")),
                        WillSendHttpRequest.builder()
                                .request(HttpRequest.builder().method("GET").path("/users/foo").build())
                                .response(HttpResponse.builder()
                                        .status(200)
                                        .body(Map.ofEntries(Map.entry("userId", "foo"),
                                                Map.entry("name", "john smith")))
                                        .build())
                                .build()),
        );
```
